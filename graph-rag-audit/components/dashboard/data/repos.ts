import type { Repo } from "../types";

/** Données de démo optionnelles (non branchées au hub réel). */
export const mockRepos: Repo[] = [
  { id: "1", name: "org/example-public", defaultBranch: "main" },
];
